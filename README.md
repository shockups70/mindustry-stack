# mindustry-stack

# Mindustry Stack for Portainer

This repo contains a Docker Compose file for running a Mindustry server.

## Usage
- Add this repo to Portainer via **Stacks → Add Stack → Repository URL**.
- Repository URL: `https://github.com/<your-username>/mindustry-stack.git`
- Compose Path: `docker-compose.yml`
